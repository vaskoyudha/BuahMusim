# Issues & Gotchas

## [2026-02-20] Windows Platform
- Running on Windows (win32) — shell commands use backslashes but code uses forward slashes
- pnpm must be installed globally: check with `pnpm --version`
